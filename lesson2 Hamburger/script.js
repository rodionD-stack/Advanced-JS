class Hamburger {
    constructor (size, stuffing) {
        this.size = size;
        this.stuffing = stuffing;
    }

    /**
     * Метод добавляет добавку.
     * @param {string} topping вид добавки
     */
    addTopping(topping) {
        
    }

    /**
     * Метод удаляет добавку.
     * @param {string} topping вид добавки.
     */
    removeTopping(topping) {

    }

    /**
     * Метод возвращает список добавок.
     * @param {string} topping вид добавки.
     */
    getToppings(topping) {
        
    }

    /**
     * Метод возвращает значение размера гамбургера
     */
    getSize() {
        
    }
    /**
     * Метод возвращает информацию о начинке гамбургера.
     */
    getStuffing() {
    
    }


    /**
     * Метод возвращает информацию о цене гамбургера.
     */
    getCalculatePrice() {

    }

    /**
     * Метод считает количество калорий в гамбургере.
     */
    calculateCalories() {

    }
}

const hamburger = new Hamburger();
hamburger.getSize();


